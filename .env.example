# ───────────────────────────────────────────────────────────────────────────────
# Porichoy — Environment Variables
#
# Copy this file to `.env` (local dev) or `.env.production` (production build).
# Never commit actual secrets. All Vite env vars are PUBLIC — they are embedded
# in the client bundle. Do NOT put private keys or server secrets here.
# ───────────────────────────────────────────────────────────────────────────────

# ── API ────────────────────────────────────────────────────────────────────────

# Base URL for all API calls (no trailing slash)
# Local dev: http://localhost:8080
# Staging:   https://api-staging.porichoy.gov.bd
# Prod:      https://api.porichoy.gov.bd
VITE_API_BASE_URL=http://localhost:8080

# Timeout for API requests in milliseconds (default 15000)
VITE_API_TIMEOUT_MS=15000

# ── Auth ───────────────────────────────────────────────────────────────────────

# Absolute session max duration in milliseconds (8 hours = 28800000)
VITE_SESSION_MAX_MS=28800000

# Inactivity timeout in milliseconds (15 minutes = 900000)
VITE_INACTIVITY_TIMEOUT_MS=900000

# ── Feature Flags ──────────────────────────────────────────────────────────────

# Enable verbose client-side logging (true | false). Disable in production.
VITE_VERBOSE_LOGGING=true

# Enable mock API responses for local development without a backend
VITE_USE_MOCK_API=false

# ── Analytics (optional) ───────────────────────────────────────────────────────

# Privacy-respecting analytics endpoint (e.g., Plausible, Matomo, Umami)
# Leave empty to disable
VITE_ANALYTICS_URL=
